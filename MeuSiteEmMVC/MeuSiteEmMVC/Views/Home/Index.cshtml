@model HomeModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Bem-Vindo, @Model.Name</h1>
    <p>Meu site em mvc</a>.</p>
    <span>Seu e-mail é : @Model.Email</span>
</div>
